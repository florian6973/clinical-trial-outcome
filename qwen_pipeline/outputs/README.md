# Runtime outputs

Store local indexes, adapters, predictions, evaluations, and manifests here. Do
not commit model artifacts or licensed SNOMED CT content. Each run manifest
should record input and configuration hashes, software and model versions, the
adapter identifier, and fixed split identifiers.
