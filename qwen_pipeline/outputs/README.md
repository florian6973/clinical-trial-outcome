# Runtime outputs

This directory is reserved for local indexes, adapters, predictions, evaluation
records, and manifests. Do not commit generated model artifacts or licensed
terminology. A run is publication-ready only when its manifest includes input
hashes, configuration hashes, software versions, model/adapter identifiers, and
the fixed split identifiers.
